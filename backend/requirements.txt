# IoTShield Verify — backend dependencies
#
# Pinned to minimum-compatible floors rather than exact versions so the demo
# installs cleanly on a fresh machine. SQLite comes from the standard library.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
reportlab>=4.1,<5.0
